# 部署前检查清单

## ✅ 部署前准备

### 1. 代码准备
- [ ] 确保代码已提交到 GitHub
- [ ] 测试本地构建：`./mvnw clean package -DskipTests`
- [ ] 验证 JAR 文件可以运行：`java -jar target/travel-0.0.1-SNAPSHOT.jar`

### 2. 环境变量准备
- [ ] 生成安全的 JWT_SECRET（至少 32 字符）
- [ ] 准备数据库连接信息
- [ ] 记录所有需要的环境变量

### 3. 数据库准备
- [ ] 确认数据库服务已创建
- [ ] 记录数据库连接 URL、用户名、密码
- [ ] 准备数据库迁移脚本（如果需要）

### 4. 配置文件检查
- [ ] `application.yml` 已配置多环境支持
- [ ] `Procfile` 已创建（Railway/Heroku）
- [ ] `render.yaml` 已创建（Render）
- [ ] `fly.toml` 已创建（Fly.io）

---

## 🚀 Railway 部署检查

- [ ] 已注册 Railway 账号
- [ ] 已连接 GitHub 仓库
- [ ] 已创建 MySQL 数据库服务
- [ ] 已设置环境变量：
  - [ ] `SPRING_PROFILES_ACTIVE=production`
  - [ ] `DATABASE_URL`（Railway 自动提供）
  - [ ] `DB_USERNAME`
  - [ ] `DB_PASSWORD`
  - [ ] `JWT_SECRET`
- [ ] 部署成功
- [ ] 访问应用 URL 测试

---

## 🌐 Render 部署检查

- [ ] 已注册 Render 账号
- [ ] 已连接 GitHub 仓库
- [ ] `render.yaml` 配置正确
- [ ] 数据库服务已创建
- [ ] 部署成功
- [ ] 访问应用 URL 测试

---

## ✈️ Fly.io 部署检查

- [ ] 已安装 Fly CLI
- [ ] 已登录：`fly auth login`
- [ ] 已初始化：`fly launch`
- [ ] 已创建数据库卷（如果需要）
- [ ] 已设置环境变量
- [ ] 已部署：`fly deploy`
- [ ] 访问应用 URL 测试

---

## 🔍 部署后验证

### 功能测试
- [ ] 访问首页
- [ ] 用户注册
- [ ] 用户登录
- [ ] JWT 认证
- [ ] API 调用
- [ ] 文件上传（如果配置了云存储）
- [ ] 数据库操作

### 安全检查
- [ ] HTTPS 正常工作
- [ ] CORS 配置正确
- [ ] 敏感信息未暴露
- [ ] JWT 密钥足够安全

### 性能检查
- [ ] 响应时间正常
- [ ] 数据库连接正常
- [ ] 静态资源加载正常

---

## 🐛 常见问题排查

### 应用无法启动
1. 检查日志输出
2. 验证环境变量是否正确
3. 检查数据库连接
4. 确认端口配置

### 数据库连接失败
1. 验证 `DATABASE_URL` 格式
2. 检查数据库服务是否运行
3. 确认防火墙规则
4. 验证用户名密码

### 静态资源无法访问
1. 检查 Spring Boot 静态资源配置
2. 验证文件路径
3. 检查 CORS 配置

### JWT 认证失败
1. 验证 `JWT_SECRET` 是否设置
2. 检查 JWT 过期时间
3. 确认前端请求头格式

---

## 📝 部署后维护

- [ ] 定期备份数据库
- [ ] 监控应用日志
- [ ] 检查资源使用情况
- [ ] 更新依赖包
- [ ] 安全补丁更新

