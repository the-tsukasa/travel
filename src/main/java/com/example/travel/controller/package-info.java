/**
 * Controller Package
 *
 * English:
 * Contains all REST controllers for handling HTTP requests.
 * Each controller corresponds to a specific business domain such as User, Admin, or Spot.
 *
 * 中文：
 * 包含所有用于处理 HTTP 请求的 REST 控制器。
 * 每个控制器负责不同的业务领域，如用户、管理员或景点模块。
 *
 * Dependencies / 依赖关系：
 * - Depends on: service, dto
 * - Used by: Spring MVC
 *
 * @author
 * @since 1.0
 */
package com.example.travel.controller;
