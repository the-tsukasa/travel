/**
 * Entity Package
 * <p>
 * English:
 * Contains all JPA entities representing database tables.
 * Each entity corresponds to a specific domain model in the system.
 * <p>
 * 中文：
 * 包含所有 JPA 实体类，对应数据库中的表结构。
 * 每个实体类表示系统中的一个业务领域模型。
 * <p>
 * Dependencies / 依赖关系：
 * - Used by: repository, service
 *
 * @author
 * @since 1.0
 */
package com.example.travel.entity;

