# Render 部署检查清单

## ✅ 部署前检查

### 代码准备
- [ ] 所有代码已提交到 GitHub
- [ ] 本地测试通过（前端和后端都能正常运行）
- [ ] 没有未提交的更改

### 配置文件
- [ ] `render.yaml` 已更新（包含前端构建步骤）
- [ ] `application.yml` 生产环境配置正确
- [ ] `frontend/vite.config.js` 构建输出路径正确

### 环境变量准备
- [ ] 已生成 `JWT_SECRET`（至少32字符的强随机字符串）
- [ ] 已准备好 `CORS_ALLOWED_ORIGINS`（部署后获取实际 URL）

---

## 🚀 部署步骤

### 1. 在 Render 创建服务
- [ ] 登录 Render Dashboard
- [ ] 创建 Web Service（从 GitHub 仓库）
- [ ] 创建 PostgreSQL 数据库

### 2. 配置环境变量
- [ ] `SPRING_PROFILES_ACTIVE` = `production`
- [ ] `JWT_SECRET` = 你的强随机密钥
- [ ] `CORS_ALLOWED_ORIGINS` = `https://your-app.onrender.com`（部署后更新）
- [ ] 数据库相关变量（自动配置，无需手动设置）

### 3. 开始部署
- [ ] 保存配置
- [ ] 等待构建完成（5-10分钟）
- [ ] 检查构建日志，确认无错误

---

## 🔍 部署后验证

### 基本功能
- [ ] 应用可以访问（无 404 错误）
- [ ] 首页正常显示
- [ ] 注册功能正常
- [ ] 登录功能正常
- [ ] 导航栏正常显示

### API 功能
- [ ] API 请求正常（检查浏览器 Network 标签）
- [ ] 没有 CORS 错误
- [ ] JWT 认证正常

### 数据库
- [ ] 数据库连接正常
- [ ] 数据可以正常保存和读取
- [ ] 检查数据库表是否已创建

### 文件上传（如果使用）
- [ ] 文件上传功能正常
- [ ] 上传的文件可以正常访问

---

## 🔧 常见问题快速修复

### 构建失败
1. 检查构建日志中的错误信息
2. 确认 Node.js 和 Java 版本兼容
3. 检查依赖是否正确安装

### 数据库连接失败
1. 检查数据库服务是否已启动
2. 确认 `DATABASE_URL` 环境变量
3. 检查数据库用户名和密码

### CORS 错误
1. 更新 `CORS_ALLOWED_ORIGINS` 环境变量
2. 确保包含完整的 URL（包括 https://）

### 前端无法加载
1. 检查前端构建是否成功
2. 确认静态资源路径正确
3. 检查浏览器控制台错误

---

## 📝 部署后操作

### 首次部署后
- [ ] 将 `DDL_AUTO` 环境变量改为 `validate`
- [ ] 更新 `CORS_ALLOWED_ORIGINS` 为实际 URL
- [ ] 测试所有主要功能

### 后续更新
- [ ] 代码更新后推送到 GitHub
- [ ] Render 自动检测并部署
- [ ] 验证更新后的功能

---

## 🎯 成功标志

如果以下所有项都正常，说明部署成功：

✅ 应用可以正常访问  
✅ 用户可以注册和登录  
✅ 数据可以正常保存  
✅ 没有控制台错误  
✅ 健康检查端点返回正常  

---

**部署完成后，记得保存你的应用 URL 和重要配置信息！**

